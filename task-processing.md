## 実行中タスク

**ソース:** `tasks/prd-inserted-tasks.md`

> - [ ] 以下のタスクが修正されきってない。
>     - [x] コードがレンダーされていなさそう。バックティック？の囲みマークがそのまま出てきてしまっている。
>     - そもそもバックティックではなく、 `**` 同様に禁止で、 `<code>` を利用すべきとしたはずだ。自動チェックの対象に入れて検査されるべきだ。

### テスト計画

- `src/lib/reference/referenceContent.test.ts` にバックティック検出テストを追加
  - すべてのエントリのtitle, summary, body（全ロケール）でバックティック文字が使われていないことを検証
  - 既存の `**` 禁止テストと同様のパターン

### ストーリー計画

- UI変更なし（データ修正のみ）
- ブラウザで修正後のリファレンスページを確認

### 実装計画

1. referenceContent.test.ts にバックティック禁止テストを追加（先にテスト → 失敗確認）
2. referenceContent.ts の全バックティックを `<code>` タグに置換
3. テスト再実行 → パス確認
