## 現在のタスク

**出典:** `tasks/inserted-tasks.md` 1行目

> タブローにおけるノードは、論理式単体ではなく、その列(集合)なのであるから、論理式列(または専用にさらに集合管理を作る)によって、内部的にも列で管理されるべきで、カンマ区切りで管理されるべきではない

### 今回のスコープ（段階的改善の第1歩）

`splitSequentText` の `text.split(",")` を括弧を考慮したスプリッターに置き換え、P(x,y) のような多引数述語を TAB クエストで使えるようにする。

### テスト計画

- `tabApplicationLogic.test.ts` に `splitSequentText` の括弧考慮テストを追加
  - `"P(x,y), Q(z)"` → `["P(x,y)", "Q(z)"]`
  - `"∀x(P(x,y) → Q(x,z))"` → 1要素
  - ネスト括弧、空文字列、既存テスト互換性確認

### ストーリー計画

- UI変更なし（内部ロジックの改善のみ）
