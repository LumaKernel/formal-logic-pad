## 実行中タスク

**出典:** `tasks/prd-inserted-tasks.md` line 10-11

> - [ ] スクリプトエディタは、作業中のファイルワークスペースみたいな概念を導入しよう (VSCodeのような)
>   - [ ] 新規のファイルはUnnamedのような扱いで、ライブラリを開くと、immutableなファイルとして別で開いて、変更を破壊しない

### 今回のスコープ

ワークスペースの純粋ロジック層（scriptWorkspaceState.ts）を作成する:
- 複数の「タブ」を管理するデータモデル
- Unnamed（新規）ファイルの作成
- ライブラリテンプレートをimmutableとして開く
- アクティブタブの切り替え
- タブの閉じる操作
- 編集状態の管理（modified/unmodified）

### テスト計画

- `scriptWorkspaceState.test.ts` に純粋ロジックのユニットテスト
  - タブ作成（Unnamed, ライブラリ）
  - アクティブタブ切り替え
  - タブ閉じる
  - コード編集による状態変化
  - ライブラリタブのimmutability

### ストーリー計画

- 今回はロジック層のみなのでストーリーなし（UI統合は次のイテレーション）
