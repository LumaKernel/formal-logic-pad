## タスク: `QuestCompleteAt01FullFlow` に中間 not-proved assert 拡充

**出典:** `tasks/quest-intermediate-asserts.md` > FullFlow ストーリー > 5番目

### 内容

`QuestCompleteAt01FullFlow` の play 関数に中間 `"0 / 1"` assert を追加。step1, step2後にassert。step3(α規則F∨)後はゴール自動達成のため除外。
